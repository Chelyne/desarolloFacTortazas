import { BuscarMarcaPipe } from './buscar-marca.pipe';

describe('BuscarMarcaPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarMarcaPipe();
    expect(pipe).toBeTruthy();
  });
});
